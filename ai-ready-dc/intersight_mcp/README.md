Note that intersight-rest-api has to be cloned to the same directory as intersight_api.py for api call functionality to work.

# To test MCP server with MCP inspector
Step 1: Change virtual environment by executing 'source .venv/bin/activate' ensuring MCP utilities are installed in the environment
Step 2: Execute 'mcp dev intersight_api.py' to start mcp inspector server